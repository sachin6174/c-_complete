//#include <iostream>
#include<stdio.h>
//compile://g++ test_code.cpp
//run://a.exe < input.txt > output.txt
#include<bits/stdc++.h>
#define ll  long long
#define vec vector  
#define ar array  
//just Throw hash map
//Pick the global decleration
//delete the dynamic allocation
//os_base::sync_with_stdio(0); 
//cin.tie(0);  
using namespace std;
int fac[100];
int expo[100];
int it;// whenever we decleare global varible or array it is 0 automatically placed in side the array or variable

// cout<<it;// we can not print any thing in global context
void my_machine(){
    int d=
    
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    
    return 0;
}
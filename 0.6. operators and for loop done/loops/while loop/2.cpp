//#include <iostream>
#include<stdio.h>
//compile://g++ test_code.cpp
//run://a.exe < input.txt > output.txt
#include<bits/stdc++.h>
#define ll  long long
#define vec vector  
#define ar array  
//just Throw hash map
//Pick the global decleration
//delete the dynamic allocation
//os_base::sync_with_stdio(0); 
//cin.tie(0);  
using namespace std;
// return 0; // only a function can return

void my_machine(){

    
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int count;
    int l;

    // for (; ; )
    // {
    //     cout<<3;
    // }
    
    5;// is true;// means pust 5 to any memory location in ram

    // cout<<&4;// this is wrong as we are just allowed to get address of an variable
    int lv1{ 42 }; // Object
    int lv2(3); // Object
    cout<<lv1;

   double a=4.5;


    for (float i[]={1,3,4,45,5,5,4},s,l=0 ; a>4,a<5 /* here comma act as or a>4||a<5  */;l++,s++ ) // ek data type kar bahut sare declerations can be done // explicit data structure not allowed (stl notallowed ) only builtin allowed
    {
        cout<<4;
    }
     void k(int l); // what is this


     {
         cout<<6;
     }
     {  int sachin;
          cout<<6;}
        // cout<<sachin; not accessible as not in a scope

    //  k();
    return 0;
}
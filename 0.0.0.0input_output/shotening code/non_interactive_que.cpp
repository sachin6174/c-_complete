#include <iostream>
#include <climits>
#define endl '\n'
#define ll long long

using namespace std;

int32_t main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); // these two lines are important for the it stop syncronisation and also it do not flushes the buffer or input stream frequently
    // it makes our input fast and saves us from tle

    //  make sure that if question need frequent input then be sure that we need to flush the input stream so we need to make sure we use endl;
    // and making look at tie and synchronisation function

    return 0;
}
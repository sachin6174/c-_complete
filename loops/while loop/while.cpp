//#include <iostream>
#include<stdio.h>
//compile://g++ test_code.cpp
//run://a.exe < input.txt > output.txt
#include<bits/stdc++.h>
#define ll  long long
#define vec vector  
#define ar array  
//just Throw hash map
//Pick the global decleration
//delete the dynamic allocation
//os_base::sync_with_stdio(0); 
//cin.tie(0);  
using namespace std;

void my_machine(){

    
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    while (1<"a23"[0]){ // we can not compare a number with an array but we can caompare it with a single element of the array
       // if we do not write anything here we will stuck in aendless loop of doing nothing
       // appears that our system has been hanged
       break; // so break will help to come out from the loop
    }

    while (1<5,1<3)   //comma acts as and (&&) not or 
    {
        cout<<true<<endl;
        break;
      
    }
    for (int i = 0,a[10],l='c'; 1>2,1<2; i++,l++) //comma acts as or (||) not and 
    {
        // if we do not write anything here we will stuck in aendless loop of doing nothing
       // appears that our system has been hanged
    }
    
    
   

    return 0;
}
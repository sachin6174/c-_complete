//#include <iostream>
#include <stdio.h>
// compile://g++ test_code.cpp
// run://a.exe < input.txt > output.txt
#include <bits/stdc++.h>
#define ll long long
#define vec vector
#define ar array
// just Throw hash map
// Pick the global decleration
// delete the dynamic allocation
// os_base::sync_with_stdio(0);
// cin.tie(0);
using namespace std;

void my_machine()
{
}
int sac;
int sac1;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    cout << !0 << endl;
    cout << !1 << endl
         << endl;

    // if there is any  " 1 " in the binary representation of a number or a intezer then it  is considered as true (for any boolean operation or storing that intezer as a boolean )
    cout << !-2 << endl;
    cout << !-1 << endl;
    cout << !2 << endl;
    cout << !3 << endl;
    cout << !-3.5 << endl;
    cout << !3.0 << endl
         << endl;

    cout << !1.0 << endl;
    cout << !0.0 << endl; // the binary expression of 0.0 also does not have any " 1 " in its binary string representation.

    cout << endl
         << sac1;

    return 0;
}
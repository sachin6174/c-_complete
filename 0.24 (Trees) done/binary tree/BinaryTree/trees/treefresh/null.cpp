//#include <iostream>
#include<stdio.h>
//compile://g++ test_code.cpp
//run://a.exe < input.txt > output.txt
#include<bits/stdc++.h>
#define ll  long long
#define vec vector  
#define ar array  
//just Throw hash map
//Pick the global decleration
//delete the dynamic allocation
//os_base::sync_with_stdio(0); 
//cin.tie(0);  
using namespace std;

void my_machine(){

    
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout<<int(NULL)<<endl;//which is zero so false /// it the first ascci character which has hashing num as 0;ascaai(NULL)=0;
    int *i=NULL;
    if(i){
        return true;
    }else{
        cout<<false;
    }
    
    return 0;
}